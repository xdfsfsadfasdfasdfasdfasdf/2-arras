let arraytest = ["test", "testing", "haha test"]
const randtest = Math.floor(Math.random() * arraytest.length)

module.exports = {
    spawn_message: (randtest, arraytest[randtest]),
    mode: "tdm",
    teams: 1,
    room_setup: ["room_tiles_test"]
}