# Compatible image file extensions:
* .jpeg / .jpg
* .png

If the compatible image types is not any of those, then the server will assume it's a video. Make sure that the video file is readable and compatible.
